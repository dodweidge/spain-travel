# 本地打开旅行网页

双击项目根目录中的 **启动本地网页.cmd**。浏览器会打开：

http://127.0.0.1:8765/

启动器使用 Node.js，并优先使用电脑已安装的版本；也支持本机 Codex 自带的 Node.js。预览服务只监听本机回环地址，重复启动会复用同一项目的服务。网页文件保存后刷新浏览器即可看到修改。请不要再双击 `index.html` 来使用谷歌互动地图。

## 若仍然无法加载谷歌地图

1. 确认当前浏览器能直接访问 https://maps.google.com/ 。本地预览无法解决网络本身无法连接 Google 的问题。
2. 若提示授权失败，在 Google Cloud Console → API 和服务 → 凭据中，找到本项目使用的浏览器地图密钥。在“网站”限制中保留原有域名，并添加 `http://127.0.0.1:8765/*`。若改用 localhost，也需添加 `http://localhost:8765/*`。
3. 确认项目已启用 Maps JavaScript API、有效结算及可用配额。周边地点搜索另需按 Google 的要求启用 Places API (New)。
4. 保存后稍等片刻，再刷新网页。浏览器控制台中的 `RefererNotAllowedMapError` 表示来源地址未获允许；`ApiNotActivatedMapError` 表示 API 未启用；`BillingNotEnabledMapError` 表示结算未启用。

无需取消密钥的网站限制，也无需将密钥改为无限制。

Google 官方说明：
- https://developers.google.com/maps/api-security-best-practices
- https://developers.google.com/maps/documentation/javascript/error-messages
